\titleformat{\chapter}[display]
{\centering\bfseries\Large}
{BAB \thechapter}
{0pt}
{\Large\MakeUppercase}

\titlespacing*{\chapter}
{0pt}
{-20pt}
{20pt}

% ============================================

\makeatletter
\newcommand{\judulCover}{
\judulUtamaID
\ifx\studiKasusID\@empty
\else
\\
{\fontsize{14pt}{18pt}\selectfont\bfseries
(STUDI KASUS: \studiKasusID)}
\fi
}
\makeatother

% ============================================

\makeatletter
\newcommand{\judulSkripsiID}{
\judulUtamaID
\ifx\studiKasusID\@empty
\else
\newline
(STUDI KASUS: \studiKasusID)
\fi
}
\makeatother

% ============================================

\makeatletter
\renewcommand{\@dotsep}{4.5}
\makeatother

% ============================================

\makeatletter
\newcommand{\judulSkripsiEN}{
\judulUtamaEN
\ifx\studiKasusEN\@empty
\else
\newline
(CASE STUDY: \studiKasusEN)
\fi
}
\makeatother


\makeatletter
\newcommand{\judulSkripsiIDPlain}{
\judulUtamaID
\ifx\studiKasusID\@empty
\else
\space (CASE STUDY: \studiKasusID)
\fi
}
\makeatother

% ============================================
\pagestyle{fancy}
\fancyhf{}

\fancyfoot[L]{\footnotesize \fakultas\ | \programStudi}
\fancyfoot[R]{\thepage}

\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

% ===== Agar halaman awal BAB tetap pakai footer =====
\fancypagestyle{plain}{
\fancyhf{}
\fancyfoot[L]{\footnotesize \fakultas\ | \programStudi}
\fancyfoot[R]{\thepage}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
}

% INDONESIAN LABEL
\renewcommand{\contentsname}{DAFTAR ISI}
\renewcommand{\listfigurename}{DAFTAR GAMBAR}
\renewcommand{\listtablename}{DAFTAR TABEL}
\renewcommand{\figurename}{Gambar}
\renewcommand{\tablename}{Tabel}

\setcounter{secnumdepth}{3}

\DefineBibliographyStrings{english}{
  bibliography = {DAFTAR PUSTAKA}
}